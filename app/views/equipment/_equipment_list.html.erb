<div class="container">
    <div class="text-center">
      <% if @user.nil? %>
        <h3>Tool List</h3>
      <% else %>
        <h3><%= @user.name %>'s Tool List</h3>
      <% end %>
    </div>
    <!--<div class="row table-responsive-sm">
      <table class="table table-striped table-light">
        <thead class="background-base text-light">
          <tr>
            <th scope="col">Name</th>
            <th scope="col">Brand</th>
            <th scope="col">Description</th>
            <th scope="col"></th>
          </tr>
        </thead>
        <% @equipment.each do |eq| %>
        <tr>
          <td><%= eq.name %></td>
          <td><%= eq.brand %></td>
          <td><%= eq.description %></td>
          <td><div class="p-1"><%= link_to 'View', user_equipment_path(eq.user_id, eq.id) %></div>
          <div class="p-1"><%= link_to 'Edit/Delete', edit_user_equipment_path(eq.user_id, eq.id) %></div></td>
          <% end %>
        </tr>
      </table>
    </div>-->
    <%= render "equipment" %>
</div>